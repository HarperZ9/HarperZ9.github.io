# Keep generated site documents stable across Windows and GitHub Pages.
*.html text eol=lf

# Downloadable career documents are byte-verified release artifacts.
*.pdf binary

# Spatial world packages are byte-receipted: no EOL conversion, ever.
art/spatial/** -text
